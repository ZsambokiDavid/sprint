version: '3'
#volumes:
#  data:
services:
  python:
    image: python
    restart: always
    volumes:
      - /root/pcode/app.py=/code/app.py
#    command: "/root/pcode/app.py"
    entrypoint: python /code/app.py
    networks:
    - nextcloud-network

    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.hostname == ${NODENAME}

networks:
  nextcloud-network:
    external: true
