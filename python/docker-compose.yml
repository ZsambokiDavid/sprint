version: '3'
#volumes:
#  data:
services:
  python:
    image: python
    restart: always
    environment:
      - /root/pcode/code.py=code.py
#    command: "/root/pcode/code.py"
    entrypoint: python code.py
    networks:
    - nextcloud-network

    deploy:
      mode: replicated
      replicas: 1
      placement:
        constraints:
          - node.hostname == ${NODENAME}

networks:
  nextcloud-network:
    external: true
